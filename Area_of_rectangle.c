#include<stdio.h>
int main(){
    int length=45;
    int breadth=67;
    int area;
    area=length*breadth;
    printf("Length is: %d\n",length);
    printf("Breadth is: %d\n",breadth);
    printf("Area of the rectangle is: %d",area);
    return 0;
}